import rootDefault from './_root-default-layout'
import rootEmpty from './_root-empty-layout'
import nested from './_nested'
import redirect from './_redirect'

export default {
  ...rootDefault,
  ...rootEmpty,
  ...nested,
  ...redirect,
}
